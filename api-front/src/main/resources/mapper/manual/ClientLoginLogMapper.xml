<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joycity.joyclub.apifront.mapper.manual.ClientLoginLogMapper">

   <!-- <insert id="addLog">
        insert into client_login_log(client_id,project_id<if test="from!=null">,`from`</if>)
        values(#{clientId},#{projectId}<if test="from!=null">,#{from}</if>)
    </insert>
    <insert id="addLogWithSubProject">
       insert into client_login_log(client_id,project_id,sub_project_id<if test="from!=null">,`from`</if>)
        values(#{clientId},#{projectId},#{subProjectId}<if test="from!=null">,#{from}</if>)
    </insert>-->
</mapper>